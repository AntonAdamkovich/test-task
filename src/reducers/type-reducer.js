export default function typeReducer (state, action) {
    switch (action.type) {
    case 'NEXT':
    // current page and shown items
        break;
    case 'PREVIOUS':
        console.log();
        break;
    // current page and shown items
    default:
        return state;
    }
}
